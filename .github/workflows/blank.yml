name: CI

on:
  workflow_dispatch:
    inputs:
      string:
        type: string
        required: true
      boolean:
        type: boolean
        required: true
      choice:
        type: choice
        options:
          - alpha
          - bravo
          - charlie
        required: true
      environment:
        type: environment
        required: true

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Run a one-line script
        env:
          INPUTS: ${{ toJSON(github.event.inputs) }}
        run: echo "$INPUTS"
